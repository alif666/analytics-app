services:
  dbservice:
    image: mysql:latest
    container_name: analyticsdb
    restart: unless-stopped
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: analytics
    volumes:
      - type: bind
        source: C:/docker-data/analytics
        target: /var/lib/mysql